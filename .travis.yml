language: rust

cache:
  directories:
    - $HOME/.cargo

rust:
    - stable
    - nightly
    - beta

env:
  - RUSTFLAGS="-D warnings"

os:
    - linux
    - osx

script:
    - env
    - rustup component add rustfmt
    - cargo fmt -- --check
    - cargo test
    - cargo doc
